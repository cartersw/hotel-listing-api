AddTransient - created at time of request, terminated instantly
AddScoped - created at time of request, disposed at the end of request
AddSingleton - created at time of request, disposed at the end of application

dotnet ef migrations add ... -> queues up a db change that then can be processed with -> dotnet ef database update

DI = class receives objects it depends on from external source rather than creating them itself.
Implementation for interfaces comes from outside source (DI container)
no Di = HotelService -> CountryService
DI = HotelService -> ICountryService <- CountryService

dotnet user-secrets set "blah:blah" "value" -> sets a secret in the user secrets store

remove whole line completions 

calling method in override using base. means you can call base method implementation before your overriden implementation

b.HasIndex(k => k.Key).IsUnique();
creates an index: seperate entity from table using the given column as the key for faster reads using this key
index has unique property due to isunique() 

jwt = header.payload.(header.payload signed by securitykey)
payload and header can be decoded by anyone and changed, but they will not be able to create the proper signature at the end without the securitykey that the issuer has
jwt.io

endpoints should be hierarchical for understandability, for example a booking shouldnt exist without a hotel so
api/hotels/{hotelid}/bookings
instead of
api/bookings

policy = list of rules needed to pass
requirement = singular rule
handler = code that evaluates whether the rule passes given context

dotnet ef migrations add [migration name] --project HotelListing.Api.Domain --startup-project HotelListing.Api 
dotnet ef database update --project HotelListing.Api.Domain --startup-project HotelListing.Api

optionally you can add --context HotelListingDbContext

Query: ?pageNumber=1&pageSize=20 (at the end of endpoint url)
Implement paging to limit the number of records returned per request
Cursor based paging can help with large data sets 

Dapper can be used instead of ef core for faster operations

MemoryCache = cache data used in application, more flexibility
OutputCache = cache actual endpoint response only

Action<T> expects a function that takes a type T

https://localhost:7041/healthchecks-ui#/healthchecks

--TODO
Implement DB Api key creation
Implement DB Api key checking in apikeyvalidatorservice





